export type Producto = {
  id: string;
  nombre: string;
  precio: string;
  descripcion: string;
  fotoUri: string;
  creadoEn: number;
};

export type Resena = {
  id: string;
  descripcion: string;
  evidenciaUri: string;
  creadoEn: number;
};
